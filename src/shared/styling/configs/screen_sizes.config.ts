import type { ScreenSizesConfig } from '../types/screen_sizes_config.type';

export const SCREEN_SIZES_CONFIG: ScreenSizesConfig = {
  small: '(max-width: 399px)',
  medium: '(min-width: 400px) and (max-width: 1099px)',
  large: '(min-width: 1100px)'
};
